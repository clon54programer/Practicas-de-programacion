#ifndef excepciones_h
#define excepciones_h

#include <iostream>

using namespace std;

class UserControl
{
private:
    bool accept;

public:
    void Interacion(string x);
    bool verificacion(bool x);
};
#endif